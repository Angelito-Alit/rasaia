# credentials.yml
rest:
  # Habilitar CORS para desarrollo
  cors_origins: "*"
  # Para producción usar: cors_origins: "http://localhost:5173"

socketio:
  user_message_evt: user_uttered
  bot_message_evt: bot_uttered
  session_persistence: true